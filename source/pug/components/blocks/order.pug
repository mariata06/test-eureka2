mixin order
  section.order
    .container
      h1.order__title.title.title--h1 Доставка топлива
      .order__form.form
        form(action="https://echo.htmlacademy.ru/" method="POST")
          h2.form__title.title.title--h2 Быстрый <span>заказ топлива</span>
          .form__wrapper
            .form__input-wrapper
              label(for="name") Ваше имя
              input(type="text" id="name" name="name" required placeholder="Иван")
            .form__input-wrapper
              label(for="phone") Номер телефона
              input(type="tel" id="phone" name="phone" required placeholder="+7 (___) ___-__-__")
            button(type="submit").form__submit.btn.btn--blue-bg Заказать топливо
            .form__checkbox-wrapper
              input(type="checkbox" name="policy" id="checkbox" required)
              label(for="checkbox") Я согласен с условиями обработки данных
